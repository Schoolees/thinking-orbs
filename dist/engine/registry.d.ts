import { ModeKey } from '../presets';
import { ModeDraw } from './types';
export declare const MODE_DRAWS: Record<ModeKey, ModeDraw>;
export declare const CONTOUR_MODE_DRAWS: Record<ModeKey, ModeDraw>;
